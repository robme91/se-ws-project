package objects;

/**
 * Created by tom on 15.01.17.
 *
 * A GameBorder can be used as invisible block.
 */
public class GameBorder extends Block {
    public GameBorder(int pos_x, int pos_y, int size, boolean isBlocking) {
        super(pos_x, pos_y, size, isBlocking);
    }
}
